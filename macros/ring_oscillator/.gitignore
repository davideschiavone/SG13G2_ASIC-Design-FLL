# Layout intermediates (regenerable via `make gen-layout`)
layout/_*
layout/*.mag
layout/*.ext
# Verification / extraction outputs
verification/
netlist/
# Generated artifacts copied into the testbench dir by `make pex` / ngspice
testbenches/spice/ring_oscillator_pex.spice
testbenches/spice/*.raw
